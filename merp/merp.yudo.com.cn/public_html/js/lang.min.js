var langCode = {

    langUrl:'/JLAMPCommon/langBatchRows_prc',
    dataList:[],
    method:'http',

    getWord:function (List, vue, func) {
        this.dataList = List;
        var _wordId = [];
        for (var list in List) {
            _wordId.push(List[list])
        }
        var _wordIdListJson = JSON.stringify(_wordId);

        if(this.method == 'cache'){
            langCode.returnList(multi.getLangByCache(_wordId),List, vue, func)
        }else{
            jq.ajax({
                url: langCode.langUrl,
                data: {keys: _wordIdListJson},
                type: 'post',
                dataType: 'json',
                success: function (res) {
                    langCode.returnList(res.data,List, vue, func)
                }
            });
        }
    },
    returnList:function (res,List, vue, func) {
        for (var list in List) {
            try{
                vue[list] = res[List[list]].LabelCaption;
            }catch (e) {
                console.log('error : '+item)
            }
            console.log(list);
        }
        func();
    }
}